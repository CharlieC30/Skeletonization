### Directory Structure

- The `kimimaro/` directory is cloned from [seung-lab/kimimaro](https://github.com/seung-lab/kimimaro). 
- Only `scripts/` folder is tracked in version control; all other files are from the original repository.
- The `scripts/` folder contains custom scripts for running Kimimaro skeletonization.