### Directory Structure

- The `kimimaro/` directory is cloned from [seung-lab/kimimaro](https://github.com/seung-lab/kimimaro). Only this `scripts/` folder is tracked in version control; all other files are from the original repository.

- This `scripts/` directory contains custom scripts for running Kimimaro skeletonization.